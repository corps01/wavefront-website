/**
 * Optional: paste ONE public post or reel URL (mobile: ⋮ → Copy link).
 * We load Instagram’s official embed script for that post only.
 * Profile /embed in an iframe usually breaks with “profile may be removed” — that’s on Meta’s side.
 * Never put Graph API user tokens or app secrets in this file.
 */
window.WAVEFRONT_INSTAGRAM_POST_PERMALINK = "";
